# Kubernetes — official Python client (the right ready-made choice)
# Handles in-cluster auth, kubeconfig, CRD operations, status patching.
kubernetes==29.0.0

# GitHub GraphQL — gql with aiohttp transport
# Cleaner than raw aiohttp: parses/validates queries, handles retries.
gql[aiohttp]==3.5.0

# Argo CD — direct aiohttp (no official Python SDK exists)
aiohttp==3.9.3

# AI
anthropic>=0.40.0

# Template rendering for AI prompts
jinja2==3.1.3

# Structured logging
structlog==24.1.0

# Metrics
prometheus-client==0.19.0

# Testing
pytest==8.0.0
pytest-asyncio==0.23.5
pytest-mock==3.12.0

# Dev tooling
ruff==0.2.1
